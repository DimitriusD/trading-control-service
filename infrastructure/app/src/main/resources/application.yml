server:
  port: ${APP_PORT:8095}

market-data-service:
  base-url: ${MARKET_DATA_SERVICE_BASE_URL:http://localhost:8080}

management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics"