type: object
required:
  - instrument
  - channels
  - startImmediately
properties:
  instrument:
    $ref: './InstrumentId.yaml'
  channels:
    type: array
    minItems: 1
    items:
      $ref: "../channel/ChannelSelection.yaml"
  startImmediately:
    type: boolean